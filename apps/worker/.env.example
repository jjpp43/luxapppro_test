# Worker is not deployed yet. Earn is also gated by stores.loyalty_earn_enabled
# (default false). Do not register Lightspeed webhooks until a pilot store is chosen.

PORT=8787
SUPABASE_URL=
SUPABASE_SECRET_KEY=
LOYALTY_WEBHOOKS_ENABLED=false
LOYALTY_EARN_GLOBAL=false
LIGHTSPEED_WEBHOOK_SECRET=
